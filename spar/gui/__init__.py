"""spar gui: PySide6 dashboard-pilot for the spar engine.

Optional extra (``pip install 'spar-cli[gui]'``); this package must not be
imported by anything under ``spar/`` outside of ``spar/cli.py``'s routing,
so a system without PySide6 installed can still use every other ``spar``
subcommand untouched.
"""
